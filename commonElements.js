function solve(arr1, arr2) {
    for (let i=0; i<arr1.length; i++){
        let firstEl = arr1[1];

        for (let j=0; j,arr2.length;j++){
            let secondEl = arr2[j];

            if (firstEl===secondEl){
                console.log(firstEl);
            }
        }
    }
    
}